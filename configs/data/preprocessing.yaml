# =============================================================================
# configs/data/preprocessing.yaml
# =============================================================================

preprocessing:
  snv_normalization:
    enabled: true

  per_sample_mean_subtraction:
    enabled: false

  savitzky_golay:
    enabled: true
    window_length: 11
    polyorder: 3

  clip:
    enabled: true
    min_val: -3.0
    max_val:  3.0

  pipeline:
    - savitzky_golay
    - snv_normalization
    - clip

derivative:
  enabled: true
  window_length: 11
  polyorder: 3