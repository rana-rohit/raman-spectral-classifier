model:
  name: hybrid

  signal_length: 1000
  n_classes: 5
  in_channels: 2

  cnn_channels: [32, 64, 128]
  cnn_kernel_sizes: [7, 5, 3]

  transformer_dim: 128
  transformer_heads: 4
  transformer_layers: 2
  transformer_dropout: 0.1

  embedding_dim: 128
  dropout: 0.3
